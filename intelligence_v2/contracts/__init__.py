"""
V2 dashboard contracts — the read-only bridge between V2's database and future
Streamlit pages (mirrors V1's data/contracts.py pattern).

Intentionally EMPTY in Phase 0 (Foundation). No dashboard pages exist yet, so
there is nothing for a contract to serve. Populated starting with the Sector
Intelligence module.
"""
